export const chatPrompt = `You are Mindforge, an AI assistant. Answer concisely and helpfully.`;

export const taskExtractionPrompt = `Extract actionable tasks from the following message. Return a JSON array of tasks.`;
